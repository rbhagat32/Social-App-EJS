<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <link rel="icon" href="/images/logo.png" />
    <link rel="stylesheet" href="/styles/styles.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div
      class="bg-zinc-900 container max-w-screen-sm relative mx-auto px-4 py-10 w-full min-h-screen border border-zinc-800"
    >
      <div id="header" class="flex justify-between items-center">
        <div class="flex gap-2 items-end">
          <div class="bg-red-200 w-10 h-10 rounded-full overflow-hidden">
            <img
              class="w-full h-full object-cover object-top"
              src="<%= user.image.toString('base64') !== '' ? 
      'data:image/png;base64,' + user.image.toString('base64') : 
      '/images/placeholder.png' %>"
            />
          </div>
          <h1 class="h-fit block text-4xl"><%= user.name.split(" ")[0] %></h1>
          <% if (user.isVerified) { %>
          <div class="w-4 h-4 mb-2">
            <img src="/images/verified.png" />
          </div>
          <% } %>
        </div>
        <a
          href="/feed"
          class="block relative border border-red-500 hover:border-red-600 text-sm sm:text-md px-4 py-2 w-fit rounded-md font-semibold cursor-pointer"
          >👈 back</a
        >
      </div>

      <div id="form" class="mt-5">
        <form
          method="post"
          action="/edit/<%= post._id %>"
          class="flex flex-col gap-2"
        >
          <textarea
            required
            name="content"
            placeholder="What's on your mind ?"
            class="text-justify w-full resize-none outline-none bg-transparent px-4 py-3 rounded-md border border-zinc-400"
          >
<%= post.content %></textarea
          >
          <div class="flex gap-4 items-center justify-end">
            <input
              type="submit"
              value="Update Post"
              class="bg-blue-500 hover:bg-blue-600 px-4 py-2 w-fit rounded-md mt-2 font-semibold cursor-pointer"
            />
          </div>
        </form>
      </div>
    </div>

    <script src="/scripts/script.js"></script>
  </body>
</html>
