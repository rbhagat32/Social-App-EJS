<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register</title>
    <link rel="icon" href="/images/logo.png" />
    <link rel="stylesheet" href="/styles/styles.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div
      class="bg-zinc-900 container mx-auto px-4 py-10 w-full min-h-screen max-w-screen-sm flex flex-col items-center border border-zinc-800"
    >
      <h1 class="text-4xl mb-6">Create New Account :</h1>

      <form
        method="post"
        action="/register"
        enctype="multipart/form-data"
        class="flex flex-col gap-2 w-80"
      >
        <input
          required
          name="name"
          type="text"
          placeholder="Name"
          class="outline-none bg-transparent px-3 py-1 rounded-md border border-zinc-400"
        />
        <input
          required
          name="username"
          type="text"
          placeholder="Username"
          class="outline-none bg-transparent px-3 py-1 rounded-md border border-zinc-400"
        />
        <input
          required
          name="email"
          type="email"
          placeholder="Email"
          class="outline-none bg-transparent px-3 py-1 rounded-md border border-zinc-400"
        />
        <input
          required
          name="password"
          type="password"
          placeholder="Password"
          class="outline-none bg-transparent px-3 py-1 rounded-md border border-zinc-400"
        />

        <div class="flex justify-between mt-2">
          <label
            for="imageUpload"
            class="border text-blue-400 hover:text-blue-500 border-blue-400 hover:border-blue-500 px-4 py-2 w-fit rounded-md text-sm cursor-pointer"
            >Upload Profile Picture</label
          >
          <input
            id="imageUpload"
            type="file"
            name="image"
            class="hidden outline-none bg-transparent cursor-pointer"
          />
          <input
            type="submit"
            value="Create"
            class="bg-green-600 hover:bg-green-700 px-4 py-2 w-fit rounded-md font-semibold cursor-pointer"
          />
        </div>
      </form>
    </div>

    <script src="/scripts/script.js"></script>
  </body>
</html>
